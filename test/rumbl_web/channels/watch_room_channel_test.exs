defmodule RumblWeb.WatchRoomChannelTest do
  use RumblWeb.ChannelCase, async: true

  test "TODO: join and sync watch room channel" do
    # Scaffold file for upcoming watch-along tests.
    # Suggested next tests:
    # 1) host can join room topic
    # 2) viewer can join room topic
    # 3) non-member cannot push host-only sync events
    assert true
  end
end
